
# 产品需求文档（PRD）

## 智能小说播放器（Android版）SonicRead

| 文档版本 | 修改日期 | 修改人 | 修改说明 |
|---------|---------|--------|---------|
| V1.0 | 2026-08-06 | — | 初始版本创建 |
| V1.1 | 2026-08-06 | AI修订 | 修正静音检测技术方案（实时检测 → 播放前预分析），修正 Android 11+ 存储权限方案，播放记忆提前到 V1.0 |


---

## 一、项目概述

### 1.1 产品背景

用户拥有大量MP3格式的有声小说文件（约100个/部，每个文件大小1-2MB）。这些文件在切割时，部分文件末尾存在**静音段**（如文件总时长10分钟，但第8分钟之后内容已结束，剩下2分钟为空白静音），用户在连续收听时需要手动跳过这些静音尾段，体验不佳。

### 1.2 产品目标

开发一款轻量级Android播放器APP，核心价值在于：
- 首次启动引导用户**自选小说文件夹**（内置目录浏览器），扫描并识别其中的MP3文件
- 按文件名顺序连续播放
- **自动跳过文件尾部的静音段，无声则自动跳转下一集**，实现无缝收听体验

### 1.3 目标用户

- 习惯在通勤、运动、睡前收听有声小说的用户
- 对手机操作不太熟练，希望"打开即听、自动续播"的普通用户
- 有大量本地音频资源，不想依赖第三方在线平台的用户

### 1.4 本版本范围（V1.0）

| 包含 | 不包含 |
|------|--------|
| F1 文件夹扫描、F2 文件列表、F3 顺序播放、F4 播放控制、F5 进度控制 | 循环/随机播放（后续版本） |
| **F6 静音自动跳转（核心）** | 自定义扫描文件夹（V1.1） |
| F7 播放记忆（简易版） | 睡眠定时（V1.2） |
| F8 后台/锁屏播放 | 其他音频格式、均衡器（V2.0） |
| **F9 倍速播放（V1.0 即包含）** | — |


## 二、技术选型说明

| 项目 | 选择 | 说明 |
|------|------|------|
| 开发框架 | **uni-app** | 跨平台框架，一套代码可打包Android/iOS；支持调用原生能力；开发效率高 |
| 打包方式 | HBuilderX | 云打包或本地打包生成APK文件 |
| 音频播放核心 | `uni.createInnerAudioContext()` | uni-app原生音频API（App端为原生MediaPlayer实现），支持播放控制、进度监听、事件回调，且原生播放天然支持锁屏/后台继续 |
| 静音检测方案 | **播放前预分析（renderjs + Web Audio API 解码）** | 在播放前把MP3文件解码为PCM数据，直接算出"内容结束点"（静音起点），播放到该时间点即跳转。**不做实时音量检测**（详见 4.3 技术约束） |
| 文件访问 | `plus.android` + `java.io` | targetSdk≥29 后 `plus.io` 无法通过绝对路径访问外部存储（Android 10+ 分区存储限制，报 code:15），改用反射 Java `File`/`FileInputStream` API 扫描目录、读取文件，完全绕开该限制 |


## 三、功能需求

### 3.1 功能总览

| 编号 | 功能模块 | 优先级 | 简述 |
|------|---------|--------|------|
| F1 | 文件夹扫描 | P0 | 用户自选小说目录（内置目录浏览器），扫描该目录下的MP3文件 |
| F2 | 文件列表展示 | P0 | 按文件名数字自然排序展示所有音频文件 |
| F3 | 顺序播放 | P0 | 点击列表项开始播放，播完自动播下一首 |
| F4 | 播放控制 | P0 | 播放/暂停、上一首、下一首（500ms防抖） |
| F5 | 播放进度显示与控制 | P1 | 显示当前进度/总时长，支持拖动进度条跳转 |
| F6 | 静音自动跳转 | P0 | **核心功能**：自动跳过文件尾部静音段 |
| F7 | 播放记忆 | P1 | 记住上次播放的文件与进度，打开APP自动续播 |
| F8 | 后台/锁屏播放 | P1 | 锁屏或切后台时音频不中断（Android 配置级支持，无需自定义代码） |
| F9 | 倍速播放 | P1 | 0.5x ~ 2.0x 六档可调，设置持久化保存，无需重复设置 |
| F10 | 运行日志（调试） | P2 | 关键事件与心跳写入日志文件，用于排查后台播放问题 |

### 3.2 详细功能描述

#### F10：运行日志（调试辅助）

| 项目 | 说明 |
|------|------|
| **日志位置** | `/storage/emulated/0/Download/sonicread.log`（手机文件管理器可直接查看），APP 内顶部"📄 日志"按钮也可查看/刷新/清空 |
| **记录内容** | 启动/页面前后台切换、权限结果、扫描结果、播放/暂停/切歌、静音跳转、兜底切歌、错误、倍速设置；播放中每 15 秒记一条进度 `TICK`；每 60 秒记一条心跳 `HEARTBEAT` |
| **用途** | 排查息屏后台播放问题：息屏后若心跳/进度记录停止 → JS 逻辑层被系统冻结；若播放本身也停 → 进程被杀。日志文件超 512KB 自动清空 |
| **兜底机制** | 播放进度到达真实结尾前 1 秒仍未触发 `onEnded` 时，强制自动切下一集（防息屏后 JS 冻结导致不切歌）；自动跳转带 1.5 秒冷却防重复 |

#### F1：文件夹扫描

| 项目 | 说明 |
|------|------|
| **触发时机** | 首次启动（未选目录）或点击"选目录"按钮时弹出**内置目录浏览器**；选定目录后自动扫描；顶部"刷新"按钮可重新扫描 |
| **目录浏览器** | 从外部存储根目录（`Environment.getExternalStorageDirectory()`）开始，列出所有子文件夹（自然排序），点击进入下一级、可返回上级；面板实时显示当前目录下 MP3 数量，点"使用此目录"确认 |
| **扫描规则** | 遍历所选文件夹（一层子目录），筛选扩展名为 `.mp3` 的文件 |
| **目录记忆** | 选择的目录持久化存储，下次打开 APP 直接扫描该目录（可在"选目录"中更换） |
| **目录不存在时** | 提示"目录不存在，请重新选择"，自动弹出目录浏览器 |
| **权限不足时** | 引导用户授权存储权限（动态申请，见 4.2） |

#### F2：文件列表展示

| 项目 | 说明 |
|------|------|
| **排序规则** | 按文件名的**自然排序**（数字顺序：1,2,3...10,11，而非字典序：1,10,11,2） |
| **展示信息** | 文件名（不含扩展名）、文件总时长（mm:ss，后台异步补全，先显示列表不阻塞） |
| **当前播放状态** | 正在播放的文件在列表中以高亮/▶ 图标标识 |
| **列表交互** | 点击任意列表项 → 从该文件开始播放 |

#### F3：顺序播放

| 项目 | 说明 |
|------|------|
| **播放队列** | 当前列表中的所有文件按排序顺序组成播放队列 |
| **播放逻辑** | 当前文件播放完毕 → 自动播放下一个列表项 → 直至队列全部播完 |
| **循环模式** | V1.0 默认列表顺序播放（播完停止），不设循环/随机模式 |
| **播放起始** | 点击列表项 = 从该文件开头播放；上一首/下一首 = 切换至相邻文件 |

#### F4：播放控制

| 控件 | 功能 |
|------|------|
| ▶️ / ⏸ | 播放/暂停切换 |
| ⏮ | 上一首（回到当前列表项的前一个文件） |
| ⏭ | 下一首（跳到当前列表项的后一个文件） |
| 防抖 | 快速连点 500ms 内仅生效一次 |
| 播放状态指示 | 当前播放的文件名在底部固定显示 |

#### F5：播放进度显示与控制

| 项目 | 说明 |
|------|------|
| **进度条** | 水平进度条（slider），实时显示播放进度 |
| **时间显示** | 左侧显示已播放时间（mm:ss），右侧显示总时长（mm:ss） |
| **拖动跳转** | 用户可拖动进度条到任意位置，音频跳转到对应时间点播放 |
| **更新频率** | 每500ms刷新一次进度显示（拖动过程中暂停自动刷新，避免抖动） |

#### F6：静音自动跳转（核心功能）

> **方案说明**：uni-app 的 `InnerAudioContext` 播放的是原生层音频，renderjs 里的 Web Audio API 无法"实时监听"到它的音量（两边不通）。因此本版本采用**播放前预分析**：把 MP3 解码成 PCM 波形，一次性算出文件尾部的静音起点，播放到该时间点直接跳转。该方案更精准、零 CPU 占用、不受锁屏冻结影响。

| 项目 | 说明 |
|------|------|
| **分析时机** | 每次切到某集时触发（播放不中断，后台分析，1-2MB 文件约 1 秒内完成，远早于播放到尾部） |
| **分析过程** | ① 用 `java.io`（`FileInputStream` + `android.util.Base64`）读取文件为 base64 → ② 传给 renderjs → ③ `AudioContext.decodeAudioData()` 解码为 PCM → ④ 按 **200ms** 一块计算 RMS（均方根）音量 → ⑤ 从文件 **80%** 位置起，寻找连续 **3秒（15块）** 音量低于 **-45dB** 的第一处起点 → ⑥ 该时间点即为 `silenceStart` |
| **检测起点** | 从 80% 位置开始向后查找（相当于"最后2分钟"范围内），文件开头/中间的正常停顿一律不检测，防止误切 |
| **判定动作** | 播放中进度 ≥ `silenceStart` 时，立即停止当前播放，**自动切换至下一集** |
| **失败兜底** | 解码失败或分析异常时静音跳转自动失效，不影响正常播放（播完由 `onEnded` 正常切下一集） |
| **用户提示** | 触发自动跳转时，界面短暂显示"检测到静音，自动跳转下一集"轻提示（2秒自动消失） |

#### F7：播放记忆（简易版，V1.0 即包含）

| 项目 | 说明 |
|------|------|
| **记忆内容** | 最后播放的文件索引 + 播放进度时间点（秒） |
| **存储方式** | `uni.setStorageSync()` 存入本地 |
| **保存时机** | 切歌时立即保存索引；播放中每5秒节流保存进度 |
| **恢复逻辑** | 下次打开APP，扫描完成后自动定位到该文件并从记忆进度点继续播放（自动续播，符合"打开即听"） |
| **开关控制** | V1.0 不做开关（默认开启），V1.1 提供设置项 |

#### F8：后台/锁屏播放

| 项目 | 说明 |
|------|------|
| **功能描述** | 锁屏（息屏）或切换到其他APP后，音频继续播放不中断 |
| **技术实现** | App端 `InnerAudioContext` 为原生播放器，天然支持后台播放。只需在 HBuilderX 云打包时勾选 manifest 中的"后台运行能力 - 音乐播放"（生成 `android.permission.WAKE_LOCK`、`FOREGROUND_SERVICE` 等权限并配置前台服务类型），**无需自定义原生代码** |
| **异常处理** | 后台服务被系统杀死时，用户下次打开APP由 F7 播放记忆自动恢复进度 |

#### F9：倍速播放

| 项目 | 说明 |
|------|------|
| **功能描述** | 支持 0.5x、0.75x、1.0x、1.25x、1.5x、2.0x 六档播放速度 |
| **入口** | 底部播放器控制区"倍速"按钮（显示当前速度如 1.0x），点击弹出速度选择面板 |
| **技术实现** | `InnerAudioContext.playbackRate` 属性；切换歌曲时重新应用（防个别机型丢失设置） |
| **持久化** | 倍速设置存入本地（`uni.setStorageSync`），APP 启动时自动恢复，**无需每次重新设置** |
| **交互反馈** | 选择后立即生效，Toast 提示"倍速 1.5x"，当前档位在面板中高亮 |


## 四、非功能需求

### 4.1 性能要求

| 指标 | 要求 |
|------|------|
| APP冷启动时间 | ≤ 2秒 |
| 扫描100个MP3文件 | ≤ 1秒（列表先出，时长异步补全） |
| 播放响应延迟（点击播放到出声） | ≤ 300ms |
| 静音分析CPU占用 | 仅发生在切换播放的瞬间（<1秒），分析完成后立即释放，播放期间零占用 |
| APP安装包大小 | 无硬性要求（自用场景，用户手机存储充足） |

### 4.2 兼容性要求

| 项目 | 要求 |
|------|------|
| Android系统版本 | Android 6.0（API 23）及以上 |
| 屏幕适配 | 适配主流手机屏幕尺寸（4.7寸 ~ 6.8寸） |
| **targetSdkVersion** | **28**（Android 9）—— 主动避开 Android 10+ 的分区存储强制约束，传统 `READ/WRITE_EXTERNAL_STORAGE` 动态申请在模拟器/真机/国产 ROM 上都通用。Android 11+ 设备对 targetSdk<30 的应用仍以旧版存储模型运行（自用分发无上架需求） |
| 存储权限 | 动态申请 `READ_EXTERNAL_STORAGE` + `WRITE_EXTERNAL_STORAGE`；拒绝时显示 banner 引导用户跳应用详情手动开启 |
| 后台保活 | 支持 Android 8.0（API 26）及以上前台服务机制，通知栏常驻播放状态通知（云打包勾选"后台运行-音乐"自动生成） |

### 4.3 技术约束（重要）

| 项目 | 说明 |
|------|------|
| ⚠️ **为什么不用实时检测** | `InnerAudioContext` 是原生层播放，renderjs（webview 层）的 Web Audio API `AnalyserNode` 只能分析 webview 内部播放的音频，**拿不到原生播放的音量数据**；且锁屏后 webview 被冻结，实时检测必然失效。因此放弃"实时检测音量"路线，改为**播放前一次性预分析**，从根上规避该问题 |
| **预分析实现约束** | 分析发生在 renderjs 层：`decodeAudioData` 需传入 ArrayBuffer（由逻辑层用 `java.io` 读文件转 base64 后传入，单文件 1-2MB 在可接受范围）；分析结果通过 `$ownerInstance.callMethod()` 回传逻辑层 |
| **解码兼容** | MP3 采用标准编码时 `decodeAudioData` 可正常解码；非常规编码（如部分 MP2）解码失败时跳过静音分析，仅退化为"播完自动切" |
| **文件访问约束** | 全部用 `plus.android` 反射 `java.io.File`（扫描/建目录）与 `FileInputStream`+`android.util.Base64`（读文件转 base64）；播放器 `InnerAudioContext.src` 直接填 `file://` 绝对路径（原生播放器不受分区存储影响）。⚠️ **`plus.io` 不能用绝对路径访问外部存储**（targetSdk≥29 的 code:15 限制），本项目完全绕开 |


## 五、UI/UX 设计概要

### 5.1 页面结构

APP仅需 **1个主页面**，采用极简设计：

```
┌─────────────────────────────┐
│  📖 我的小说   [📁选目录][🔄]│  ← 顶部标题栏 + 选目录/刷新 |
├─────────────────────────────┤
│  ▶ 第001集  03:25          │  ← 文件列表（当前播放高亮）
│  第002集  04:10            │
│  第003集  02:58            │  ← 正在播放 ▶ 图标标识
│  第004集  05:33            │
│  ...                       │
│  第100集  04:22            │
├─────────────────────────────┤
│ 第003集                    │  ← 当前播放文件名（固定底部）
│ ═══●═══════════════ 02:58  │  ← 进度条 + 时间
│  ⏮    ▶    ⏭              │  ← 播放控制按钮
└─────────────────────────────┘
```

### 5.2 交互反馈

| 场景 | 反馈方式 |
|------|---------|
| 点击列表项开始播放 | 列表项高亮 + 底部控制区更新文件名 |
| 静音自动跳转下一集 | 底部显示轻提示"检测到静音，自动跳转下一集"（2秒后自动消失） |
| 无文件可播放 | 显示占位提示"请选择一个小说文件夹"，点击弹出目录浏览器 |
| 权限未授权 | 弹窗引导跳转系统设置页授权"所有文件访问" |
| 播放到队列末尾 | 显示"已全部播完"提示，播放状态停止 |


## 六、数据字典

### 6.1 播放状态数据

| 字段 | 类型 | 说明 |
|------|------|------|
| `playlist` | `Array<FileInfo>` | 当前播放列表（所有MP3文件信息） |
| `currentIndex` | `Number` | 当前播放文件在列表中的索引（从0开始） |
| `isPlaying` | `Boolean` | 是否正在播放 |
| `currentTime` | `Number` | 当前播放进度（秒） |
| `duration` | `Number` | 当前文件总时长（秒） |

### 6.2 FileInfo 对象

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `name` | `String` | 文件名（不含扩展名） | "第001集" |
| `fullName` | `String` | 完整文件名 | "第001集.mp3" |
| `path` | `String` | 文件绝对路径 | "/storage/emulated/0/小说/第001集.mp3" |
| `duration` | `Number` | 总时长（秒），扫描后异步补全 | 205（即3分25秒） |
| `silenceStart` | `Number` | 尾部静音起点（秒），-1 表示无静音或未分析 | 480 |

### 6.3 静音分析参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `分析起始位置` | 80% | 从文件 80% 位置（相当于最后2分钟）开始向后查找静音起点 |
| `检测粒度` | 200ms | 每200毫秒为一个音量块 |
| `连续静音判定时长` | 3秒 | 连续15块音量低于阈值即判定为静音段 |
| `静音阈值` | -45dB | 音量（RMS 转 dBFS）低于此值视为"静音" |


## 七、异常场景处理

| 异常场景 | 处理方式 |
|---------|---------|
| 目标文件夹不存在 | 提示"目录不存在，请重新选择"，自动弹出目录浏览器 |
| 存储权限未授予 | 引导用户跳转系统设置授权"所有文件访问"，返回后自动重扫 |
| 目标文件夹无MP3文件 | 列表为空，显示引导提示 |
| 播放过程中文件被删除 | 捕获错误，自动跳过该文件播放下一首 |
| 静音分析解码失败 | 该文件跳过静音跳转（`silenceStart = -1`），播完正常切下一集 |
| 静音检测期间用户手动拖动进度条 | 拖动进度不影响已算好的 `silenceStart`；若用户拖入静音段内，视为用户主动跳过，立即切歌（符合预期） |
| 用户快速点击上一首/下一首 | 防抖处理（500ms内多次点击仅生效一次） |
| 播放过程中接听电话 | 系统自动暂停，挂断后由用户手动恢复（不自动恢复） |
| 后台进程被系统杀死 | 下次打开APP由播放记忆自动恢复进度 |


## 八、版本规划

| 版本 | 功能范围 | 目标 |
|------|---------|------|
| **V1.0（MVP）** | F1~F6 + F7 简易播放记忆 + F8 后台播放（配置级） + **F9 倍速播放** | 核心播放体验完整可用 |
| **V1.1** | 播放记忆开关 + 用户自定义扫描文件夹（已含于 V1.0，此版本做精细化：搜索/过滤） + 列表音量管理 | 提升使用便利性 |
| **V1.2** | 睡眠定时 | 满足进阶收听需求 |
| **V2.0** | 支持更多音频格式（m4a、aac）+ 均衡器 | 扩展兼容性 |


## 九、附录：AI生成代码提示词（给开发人员）

如果你将这份需求文档交给AI来生成代码，建议使用以下提示词模板：

```
请根据以下需求，用 uni-app 框架（Vue2）帮我生成一个完整的 Android APP 页面代码（单个 .vue 文件 + 一个 renderjs 分析模块）：

功能需求：
1. 页面加载后读取上次选择的目录并扫描其中的所有 .mp3 文件；未选择过时弹出**内置目录浏览器**（java.io 列出子文件夹、可返回上级、显示 MP3 数量、"使用此目录"确认），按文件名数字自然排序展示在列表中。
2. 点击列表项开始顺序播放，使用 uni.createInnerAudioContext() 控制播放。
3. 页面底部固定显示当前播放文件名、进度条（可拖动）、播放/暂停/上一首/下一首按钮。
4. 【核心】静音自动跳转：每次切歌时，用 plus.android 反射 java.io（FileInputStream + android.util.Base64）把当前 MP3 读成 base64，传给 renderjs 模块，在 renderjs 中用 AudioContext.decodeAudioData() 解码，从文件 80% 位置起按 200ms 分块计算 RMS 音量，找连续 15 块（3秒）低于 -45dB 的起点，作为 silenceStart 回传逻辑层；逻辑层播放到该时间点自动切下一集。
5. 播放记忆：切歌时保存文件索引、播放中每5秒保存进度，启动后自动恢复并续播。
6. 需要处理 Android 存储权限申请（动态申请 READ/WRITE_EXTERNAL_STORAGE，被拒时引导跳应用详情页）。
7. 代码中请添加详细中文注释。

技术约束：
- 不要做实时音量检测（renderjs 拿不到 InnerAudioContext 的音量），必须用播放前预分析方案
- 静音分析失败时静音跳转自动失效，不影响正常播放
- 播放列表为空时显示友好的空状态提示
- 注意：不能用 plus.io 按绝对路径访问外部存储（targetSdk≥29 分区存储限制，code:15），文件读写必须用 plus.android 反射 java.io API
```
